# .env.example
# Copy this file to .env and fill in values if needed.
# Currently EduAgent runs fully locally — no API keys required.

# Ollama server URL (only override if Ollama runs on a different host)
# OLLAMA_HOST=http://localhost:11434
